{
  "name": "idempotent-submission-handler",
  "version": "1.0.0",
  "description": "An API endpoint that safely handles duplicate submissions using Redis idempotency keys.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1"
  }
}
